# 1. 各辺の長さを変数に設定します
upper_base = 10    # 上辺 (cm)
lower_base = 20    # 下辺 (cm)
height = 5         # 高さ (cm)

# 2. 台形の面積を計算します
# 公式: (上辺 + 下辺) * 高さ / 2
trapezoid_area = (upper_base + lower_base) * height / 2

# 3. 計算結果を単位付きで出力します
print(f"{trapezoid_area}cm²")
